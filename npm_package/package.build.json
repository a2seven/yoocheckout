{
    "scripts": {
        "build": "rm -rf lib/* && tsc -p ./ && rm -rf src && rm -rf node_modules && rm package-lock.json",
        "prepublishOnly": "npm build"
    },
    "dependencies": {
        "@types/axios": "^0.14.0",
        "axios": "^0.21.1",
        "uuid": "^8.3.2"
    }
}